/**
 * Единый стиль кнопок ЛК официанта (как админка): капсула + золотая обводка.
 * См. `.cabinet-premium .cabinet-waiter-btn` в globals.css.
 */
export const CABINET_WAITER_BTN = "cabinet-waiter-btn";

/** С иконкой и текстом: flex + выравнивание (как «Выйти» / «Копировать» в сайдбаре). */
export const CABINET_WAITER_BTN_INLINE =
  "cabinet-waiter-btn inline-flex items-center justify-center gap-2";

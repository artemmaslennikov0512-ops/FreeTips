# Общий .env для скриптов в scripts/ (sd-get-balance, sd-payout, sd-topup-card, sd-topup-sbp, sd-confirm, utils/sd-register и т.д.).
# Скопируй в .env и заполни значения. Скрипты загружают scripts/.env автоматически.
# Как проверить баланс и сделать вывод — см. scripts/README.md.

# Paygine API (обязательно для sd-get-balance, sd-register, sd-payout)
PAYGINE_BASE_URL=https://test.paygine.com
PAYGINE_SECTOR=9022
PAYGINE_PASSWORD=

# Номер кубышки — используется в sd-get-balance, sd-register, sd-payout, sd-topup-sbp
PAYGINE_SD_REF=1tips_r_test
# Кубышка ЮЛ: комиссия при приёме (СБП/карта) переводится сюда при переливе (relocate-one-transaction и webhook).
# PAYGINE_SD_REF_LEGAL=
# Для utils/sd-relocate.ts (перевод между кубышками): по умолчанию from/to, если не переданы аргументами
# PAYGINE_SD_REF_FROM=1tips_t_xxx
# PAYGINE_SD_REF_TO=1tips_r_test

# Таймаут запросов к Paygine (мс), необязательно
# PAYGINE_REQUEST_TIMEOUT_MS=30000

# Для sd-register: url/failurl/notify_url редиректа после оплаты (необязательно, подставятся заглушки если не заданы)
# REGISTER_URL=https://your-app.example.com/pay/success
# REGISTER_FAILURL=https://your-app.example.com/pay/fail
# REGISTER_NOTIFY_URL=https://your-app.example.com/api/v1/webhooks/paygine
# REGISTER_APP_URL=https://your-app.example.com

# Для utils/sd-topup-card-auto.ts (автозаполнение формы карты на Paygine) — только тестовая карта
PAYGINE_TEST_PAN=2200019999000007
# PAYGINE_TEST_EXPIRY=08.25
PAYGINE_TEST_CVC=983
# HEADLESS=0   — для utils/sd-topup-card-auto: 0 = показать браузер при отладке, 1 или не задано = скрыт
# OPEN_BROWSER=1  — для sd-topup-card: 1 = открыть браузер (по умолчанию только выводится htmlPath)

# Для utils/sbp-test-case.ts и sd-topup-sbp (подтверждение оплаты по СБП на тестовом стенде)
# PAYGINE_SBP_TEST_PATH=test/SBPTestCase
# PAYGINE_SBP_QRC_ID=   — если нужен qrc_id вместо order_id
# SBP_CASE_ID=150       — 150 = успешная оплата (по умолчанию)
# sd-topup-sbp: пауза (мс) перед SDRelocateFunds после SBPTestCase. По умолчанию 12000.
# PAYGINE_SBP_RELOCATE_DELAY_MS=12000
